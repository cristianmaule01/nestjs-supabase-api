meta {
  name: Delete Deck
  type: http
  seq: 13
}

delete {
  url: {{baseUrl}}/decks/{{deckId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{authToken}}
}

vars {
  deckId: your-deck-id-here
}